# @agent-harness/server

The local Agent Harness modular-monolith distribution. One package contains the Gateway, Runtime,
Node client, and `agent-harness` CLI. The service listens only on the loopback interface.

```sh
npx @agent-harness/server serve
```
