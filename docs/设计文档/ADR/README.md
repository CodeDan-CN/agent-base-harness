# Architecture Decision Records

ADR 记录会影响多个模块、难以低成本撤销或需要明确取舍的技术决策。

状态：

- `Proposed`：已提出，尚未确认，不能作为正式实现依据。
- `Accepted`：已确认，后续变更需要新 ADR supersede。
- `Superseded`：已被新 ADR 替代。
- `Rejected`：明确不采用。

每个 ADR 至少包含背景、决策、理由、影响和替代方案。
