# Example environment (no real secrets). Values here are public defaults only.
# The application data root can be overridden for development / tests.
AGENT_CLIENT_APP_DATA=
# Comma separated allowed renderer origins (dev server). Keep localhost only.
AGENT_CLIENT_DEV_SERVER_URL=http://localhost:5173